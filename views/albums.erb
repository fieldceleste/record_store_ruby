
<h3>Available Records</h3>

<h1>All Sales Vinyl</h1>

<h3>Current Albums:</h3>
<% if @albums.any? %>
<ul>
  <% @albums.each do |album| %>
    <li><a href="/albums/<%= album.id %>"><%= album.name %></a></li>
  <% end %>
  </ul>
<% else %> 
  <p>There are currently no records to display.</p>
<% end %>
<a href="/albums/new">Add a new album</a>


<form action="/albums">
  <div class="form-group">
    <label for="search" >Search!</label>
    <input id="search" name="search" class="form-control" type="text">
  </div>
  <button type="submit" class="btn">Go!</button>
</form>
</div>

<h3>Sold albums</h3>

<% if @albums.any? %>
<ul>
  <% @albums.select { |album| album.sold_albums}.each do |album|%> 
    <li><%= album.name %></li>
  <% end %>
</ul>
<% else %> 
  <p>There are currently no sold albums to display.</p>
<% end %>